# Copy this to .env.local for local dev, .env.development for dev builds, .env.production for prod builds
# React CRA reads the matching file automatically based on NODE_ENV

REACT_APP_ENV=local
REACT_APP_API_BASE=http://localhost:8000/api
REACT_APP_WS_BASE=ws://localhost:8000/ws
